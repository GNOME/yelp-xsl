yelp_xslt_dita_common_data = [
  'dita-map.xsl',
  'dita-ref.xsl',
  'dita-selectors.mod',
]

install_data(
  yelp_xslt_dita_common_data,
  install_dir: join_paths(
    pkgxsltdir,
    'dita',
    'common',
  )
)